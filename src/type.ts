export interface Benefitstype {
  title: string;
  icon: JSX.Element;
  description: string;
}

export interface ClassesType {
  id: string;
  name: string;
  description?: string;
  image: string;
}
